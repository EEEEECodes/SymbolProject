# Install the appropriate CPU or CUDA build of PyTorch separately:
# https://pytorch.org/get-started/locally/
# PyTorch is intentionally not pinned because its wheel index depends on the
# operating system, accelerator, driver, and desired CUDA runtime.

numpy>=1.26,<3
Pillow>=10.3,<13
