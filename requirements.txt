# Install the appropriate CPU or CUDA build of PyTorch first:
# https://pytorch.org/get-started/locally/
# PyTorch is not pinned here because its wheel index depends on the target device.

numpy>=1.26,<3
Pillow>=10.3,<13
opencv-python-headless>=4.10,<5
scipy>=1.12,<2
scikit-image>=0.23,<1
Shapely>=2.0,<3
resvg-py>=0.2,<1
pytest>=8.2,<10
